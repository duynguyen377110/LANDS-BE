"use strict"

const config = {
    ok: {
        statusCode: 200,
        message: "Request success"
    },
    created: {
        statusCode: 201,
        message: "Create success"
    },
    accepted: {
        statusCode: 202,
        message: 'Request accepted'
    }
}

module.exports = config